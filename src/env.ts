const { env } = process;

export const { VK_ACCESS_TOKEN, VK_GROUP_ID } = env;
export const { SMTP_HOST, SMTP_PORT } = env;
export const { IMAP_HOST, IMAP_PORT } = env;
export const { APP_PORT } = env;

export const { EMAIL_LOGIN, EMAIL_PASSWORD } = env;
